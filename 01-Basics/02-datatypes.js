// basically datatypes are of 2 types 
// Primitive - string , number, bigint, null, undefiend, symbol, boolean.

const score = 100  // number 
const isLoggedIn = false // boolean
const respond = null // null 
let userEmail; // undefiend
var userState = "odisha" // string
const sujitBankBalance = 3456789034567890345678903456789045678; // bigInt

const id = Symbol('123') // one character
const anotherId = Symbol('123') // another character


// nonprimitive or reference 
// 1. array 
const sujitCars = ["BMW", "audi", "maybeach", "porche", "nano"]

// 2. function 

const myFunc = function (){
    console.log("hello world");
}

// 3. onject

const myObj = {
    name : "sujit",
    age : 78,
    city : "puducheri"
}

